## 알아두면 유용한 깃 커맨드


```
git reset HEAD~1
# 한 단계 전 커밋으로 돌립니다. 이때 커밋한 작업 코드은 남겨둡니다.

git reset --hard HEAD~1
# 빡세게(?) 한 단계 전 커밋으로 돌립니다. 이때 커밋 코드를 남겨두지 않습니다.

git rm --cached <파일명>
# add한 파일을 되돌립니다.
```
